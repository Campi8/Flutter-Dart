import 'package:flutter/material.dart';
class Xegg extends StatelessWidget {
  const Xegg ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("X-egg"),
      ),
      body: Center(
        child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [const Text("")]),
      ),
    );
  }
}
